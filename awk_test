We know that an IP address is in the form 192.168.0.3. It is in the form of four integers(each from 0 to 255), separated by dots(for example, 192.168.0.2).
[0-9] or [:digit:] represents a match for digit from 0 to 9. {1, 3} matches one to three digits and \. matches the dot character.
Let's first go through the basic.
Treatment of special characters.
This will match the character a, followed by any character.

123 445 666 777889
